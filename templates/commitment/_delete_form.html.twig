<form method="post" action="{{ path('commitment.delete', {'id': commitment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commitment.id) }}">
    <button class="btn">Delete</button>
</form>
